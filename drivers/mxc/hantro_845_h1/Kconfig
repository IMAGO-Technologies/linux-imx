#
# Codec configuration
#

menu "MXC HANTRO(Video Processing Unit) encoder support"
	depends on ARCH_FSL_IMX8MQ || ARCH_FSL_IMX8MM

config MXC_HANTRO_845_H1
	tristate "Support for MXC HANTRO(Video Processing Unit) encoder"
	default y
	---help---
	  VPU codec device.

endmenu
